# URL веб-приложения Apps Script (Deploy → Manage deployments → Web app URL).
# Заканчивается на /exec, НЕ на /dev.
VITE_API_URL=https://script.google.com/macros/s/XXXXXXXXXXXXXXXX/exec

# Базовый путь сборки. Локально не нужен; на CI подставляется автоматически.
# VITE_BASE=/shopping/
